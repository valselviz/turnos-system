server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # SPA: cualquier ruta que no sea un archivo real cae en index.html,
    # para que el router del lado del cliente la resuelva.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
